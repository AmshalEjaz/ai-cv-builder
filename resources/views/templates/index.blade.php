@extends('layouts.app')
@section('content')
<section class="page-title"><p class="eyebrow">DESIGN YOUR STORY</p><h1>Choose a template</h1><p class="muted">Your uploaded CV content stays the same. Pick a visual style, then Ollama helps polish the wording.</p></section><div class="template-grid">@forelse($templates as $template)@php($settings = $template->settings ?? [])<a class="template-card" href="{{ route('templates.preview', $template) }}"><div class="template-preview" style="--template-accent: {{ $settings['accent'] ?? '#167a74' }}"><b>{{ strtoupper(substr($template->name, 0, 1)) }}</b><i></i><i></i><i class="short"></i></div><h3>{{ $template->name }}</h3><p>{{ $template->description }}</p><span class="template-link">Preview template →</span></a>@empty<div class="empty-state"><h3>No templates available yet</h3><p>Run the database seeder to add the starter templates.</p></div>@endforelse</div>
@endsection
